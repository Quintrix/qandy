
Quintrix and Crew Software
Multiplayer Graphics Engine


Our gfx.js engine processes text files the way a mainframe
processes punch cards, giving it a level of speed that most
engines only dream of.

The fastest file storage is the operating system's DOS, any
database is an extra layer that can only slow things down,
thus we used the file system as the database. The map is
divided into 'sectors' which can run from Aa-Az or A0-Z9,
or Aa-Z9. Each sector is given a directory and inside that
directory any items, objects, avatars, and config are stored
as files. The filename contain everything the client needs
to render that sector, so a 'dir' command reveals the entire
sector.  

The server moves players by renaming their file, or moving 
it to a new map sector directory. The server can change items
by renaming, so if a player uses a 'switch off' the server
can turn it into a 'switch on'. 

There are 36 'V" registers (Xa-X9),  'W' registers (Wa-Wz), and
36 'X' registers. The V registers are 'verbs' that are 'game
commands'. The 'X' registers are logic gates that can be used
to program 'bot players'. And the 'W' registers are used to store
variables, which can be either an item id code, or a numeral from
00-99 (we call this the 'stack', as in 'stacking inventory items').

So XnXiLaVnZa = if not inventory player-hat north nothing, which
translates to 'if item La is not in player's inventory, walk north'
The Za it the item code for 'nothing' and is used to close the
logic gate opened. As the last bytecode, it would actually be optional,
or more commands could be added to the string to execute after the north. 


    Va avatar        XaW$## add ## to W$
    Vb                   Xb 
    Vc                   Xc character selection
    Vd                 Xd## dice roll (## sides)
    Ve east            Xe$$ if $$ is a valid exit
    Vf food (eat)        Xf
    Vg               XgW*## if W$ if greater than
    Vh                   Xh
    Vi inventory       Xi$$ if item $$ is in inventory
    Vj                   Xj
    Vk                   Xk
    Vl look          XlW$## if W$ is less than ##
    Vm                 Xm$$ if item $$ is on map (dyamic items players can drop/pickup)
    Vn north             Xn not (flips next logic gate)
    Vo ore (mine)      Xo$$ if object $$ is on map (is in .gfx file)
    Vp pier (fish)       Xp 
    Vq                   Xq
    Vr                 Xr$$ read object's data file (returns Za if item no on sector)
    Vs south         XsW$## subtract ## from W$
    Vt teleport          Xt map sectors terrain type
    Vu use (item)        Xu
    Vv                   Xv
    Vw west              Xw
    Vx                   Xx
    Vy                   Xy
    Vz                 Xz$$ if item $$ is on z-location

W* = wildcard (Wa-Wz or W0-W9)
## = numeral 00-99
$$ = byte code (Aa-Zz or A0-Z0)


The very first thing the user has to click is a male or
female head to select their avatar, this calls gfxSelectAvatar(a).
We should assign 'Xc' as 'character selection' which the link
handler can call gfxSelectAvatar() when it see's this link.
Where do I find the link handler?? Is it not in gfx.js?? 

 

Item ID Codes:

A travel items
B pick-up items
C pick-up items
D pick-up items
E pick-up items
F food items
J food items
L hats
M swords
N shields 
V v-verbs (game commands)
W item registers (variables)
X x-logic (bot commands)
Y buildings (images can be bigger than 32x32)
Z usable items 
